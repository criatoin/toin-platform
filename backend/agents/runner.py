# Implementado na Task 5 — stub para evitar ImportError no router de WhatsApp

async def run_toin_agent(
    conversation: dict,
    message_text: str,
    tenant_id: str,
    adapter,
    instance_name: str,
    from_phone: str,
):
    """Placeholder — agente LangGraph implementado na Task 5."""
    pass
